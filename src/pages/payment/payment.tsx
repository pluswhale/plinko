import { FC, ReactElement } from 'react';
import PaymentTemplate from '../../features/payment-template/payment-template';

const Payment: FC = (): ReactElement => {
    return <PaymentTemplate />;
};

export default Payment;

